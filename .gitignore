node_modules/
dist/
coverage/
.usertrust/
.usertrust-gate1/
.usertools/
.usertools-gate1/
.worktrees/
*.tsbuildinfo

# AI assistant / editor configs
CLAUDE.md
.claude/
.cursorrules
.copilot/

# Internal docs (plans, specs, research)
docs/superpowers/
docs/research/

# Internal scripts
scripts/gate1-soak.ts

# OS metadata
.DS_Store

# Environment files
.env
.env.*
.env.local
.env.production
.env*.local

# Internal CI / review tooling (not part of the public OSS project)
.github/codex/
scripts/codex-review.sh
scripts/preflight.sh

# Task tracking
tasks/
.vercel/

# ── Site media (site/public/intro, site/public/demo) ──────────────────────
#
# The large encodes are working-tree deploy assets, not repo content: /promote
# runs `vercel --prod` from the main checkout where they exist, and committing
# tens of megabytes of video to a public OSS repo to serve a landing page is
# not a trade worth making.
#
# But an untracked-only directory has a failure mode: a GIT-based deploy (or a
# fresh clone) serves a page whose <video> and poster URLs all 404. So the SMALL
# assets below are COMMITTED — a poster frame and a first-frame still for the
# hero, plus the 148KB replay clip and its poster — and a Git deploy degrades to
# a poster-only hero instead of a broken one.
#
# The directory globs must be `dir/*`, never `dir/`: git does not descend into an
# excluded DIRECTORY, so a negation inside one can never be reached.
site/public/intro/*
!site/public/intro/intro-poster.jpg
!site/public/intro/intro-first.jpg
site/public/demo/*
!site/public/demo/runaway-agent.mp4
!site/public/demo/runaway-agent-poster.jpg
